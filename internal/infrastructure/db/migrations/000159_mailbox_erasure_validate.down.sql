-- Nothing to undo: a validated constraint is the same constraint, and 000158's
-- down drops all of them. Marking them NOT VALID again is not something
-- Postgres offers, and would buy nothing if it did.
SELECT 1;
